# mnist-optimiser-toy
# MNIST Optimisation Toy Problem

This repo provides a simple ML training benchmark for optimisation experiments.

## Goal
Optimise the training pipeline to improve:

- Accuracy (higher is better)
- Runtime (lower is better)

## Run Benchmark

```bash
pip install -r requirements.txt
python train.py
